# Shell-Bun Python Requirements
# 
# Note: Shell-Bun uses Python standard library modules for core functionality.
# For interactive mode, Textual is required (handles TUI rendering).
# For testing, pytest is required.
#
# To install all dependencies:
#   pip install -r requirements.txt
#
# For CI mode only (no interactive UI):
#   No external dependencies required!

# TUI framework for interactive mode (requires Python 3.8+)
textual>=0.40.0

# Testing framework
pytest>=7.0.0

